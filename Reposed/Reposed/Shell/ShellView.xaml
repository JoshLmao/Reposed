<Window x:Class="Reposed.Shell.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Reposed.Shell"
        xmlns:menuBar="clr-namespace:Reposed.Menu"
        xmlns:outputLog="clr-namespace:Reposed.OutputLog"
        mc:Ignorable="d"
        d:DesignHeight="400" d:DesignWidth="600">
    <Grid Background="{DynamicResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <menuBar:MenuView DataContext="{Binding MenuViewModel}"/>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

            <outputLog:OutputLogView Grid.Row="1" DataContext="{Binding OutputLogViewModel}"/>
        </Grid>
    </Grid>
</Window>
